version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  
  build:
    commands:
      - echo "Validating configuration..."
      - npm run validate

artifacts:
  files:
    - config/**/*
    - schema/**/*
