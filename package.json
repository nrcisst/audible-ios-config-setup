{
    "name": "audible-ios-config",
    "version": "1.0.0",
    "description": "Configuration for Audible iOS app",
    "scripts": {
        "validate": "ajv -c ajv-formats validate -s schema/validator.json -d config/main-ios-config.json"
    },
    "dependencies": {
        "ajv": "^8.12.0",
        "ajv-cli": "^5.0.0",
        "ajv-formats": "^2.1.1"
    }
}
